{
  "name": "@game/middleware",
  "version": "1.0.0",
  "description": "Middleware for a generic game, to be imported into any frontend",
  "main": "build/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "sh scripts/build.sh",
    "build:devnet": "cross-env NODE_ENV=devnet sh scripts/build.sh",
    "build:prod": "echo \"Error: no prod environment specified\" && exit 1",
    "lint": "prettier --write '**/*.ts'"
  },
  "keywords": [],
  "author": "PaimaStudios",
  "dependencies": {
    "@game/game-logic": "1.0.0",
    "@game/utils": "1.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.4",
    "esbuild-plugin-polyfill-node": "^0.3.0"
  }
}
