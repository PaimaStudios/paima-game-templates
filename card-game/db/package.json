{
  "name": "@game/db",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc-alias -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "npx pgtyped -w -c pgtypedconfig.json",
    "start:pglite": "tsx src/pglite.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@pgtyped/runtime": "2.3.0",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.1.5",
    "@pgtyped/cli": "^2.3.0",
    "@types/pg": "^8.10.9",
    "pglite-server": "0.1.2",
    "tsc-alias": "^1.8.8"
  }
}